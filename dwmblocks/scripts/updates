#!/bin/sh

updates="$(aptitude search '~U' | wc -l)"
# updatesaur="$(yay -Qum 2> /dev/null | wc -l)"
# updates=$(("$updatesarch" + "$updatesaur"))
icon="🛠️"
printf " %s %s \\n"  "$icon " "$updates"
